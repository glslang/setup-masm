# See https://github.com/krzema12/github-actions-typing
inputs:
  vswhere-path:
    type: string
  vs-version:
    type: string
  vs-prerelease:
    type: boolean
  vs-architecture:
    type: enum
    name: Architecture
    allowed-values:
    - x86
    - x64

outputs:
  masmPath:
    type: string
